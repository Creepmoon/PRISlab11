server:
  port: 8088

spring:
  application:
    name: ar-session-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/edusphere_ar
    username: ${DB_USER:edusphere}
    password: ${DB_PASSWORD:edusphere}
  jpa:
    hibernate:
      ddl-auto: update
