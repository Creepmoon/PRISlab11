server:
  port: 8086

spring:
  application:
    name: grading-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/edusphere_grading
    username: ${DB_USER:edusphere}
    password: ${DB_PASSWORD:edusphere}
  jpa:
    hibernate:
      ddl-auto: update
